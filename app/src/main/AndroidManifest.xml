<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapp">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/tubiao1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/tubiao1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- Android 7.0 照片、APK下载保存路径-->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.myapp.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


        <activity android:name="com.example.myapp.HistoryToPictuerinformation"></activity>
        <activity
            android:name="com.example.myapp.MainActivity"
            android:label="@string/title_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 多个界面需要注册Activity -->
        <activity android:name="com.example.myapp.ui.ComponentsActivity" />
        <activity android:name="com.example.myapp.TakePhotoAndChooseAblum" />
        <activity android:name="com.example.myapp.TakephotoOrAblum" />
        <activity android:name="com.example.myapp.ui.History" />
        <activity android:name="com.example.myapp.AboutActivity" />
    </application>

</manifest>